[project]
name = "python-for-animation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Samuel Igbinijesu", email = "igbinijesusamuel@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
python-for-animation = "python_for_animation:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
